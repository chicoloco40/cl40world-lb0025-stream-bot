# 1. Go to your bot folder
cd /root/cl40-lb-international-b2c-bot   # or wherever your bot files are

# 2. Initialize git (if not already)
git init

# 3. Add the remote (your new repo)
git remote add origin https://github.com/chicoloco40/cl40world-lb0025-stream-bot.git

# 4. Add files
git add .

# 5. Commit
git commit -m "Add CL40 World Stream Alert Bot with official links"

# 6. Push to main
git push -u origin main
